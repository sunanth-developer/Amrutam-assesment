export const ENV = {
  API_TIMEOUT_MS: 8000,
  MOCK_FAILURES: __DEV__,
  MOCK_FAILURE_RATE: 0.05,
  MOCK_SLOW_NETWORK_MS: 2000,
  CACHE_TTL_MS: 1000 * 60 * 30,
  SYNC_INTERVAL_MS: 1000 * 30,
  PAGE_SIZE: 20,
} as const;
